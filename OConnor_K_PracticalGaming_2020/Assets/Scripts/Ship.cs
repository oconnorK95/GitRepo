using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ship : MonoBehaviour
{
   //This script contains a definition of a spaceship
   //A spaceship is made up of a number of ship components
   //Select a random member of each component array to populate the appropriate empty gameobjects on the ship
   //Wings should be placed into the wings empty gameobject, thrusters into the thrust gameobject etc


    //C# does not allow multiple inheritance...
    //Merge all the abstract classes into 1 or use interfaces?
    void Start()
    {
        
    }

  
    void Update()
    {
        
    }
}
